<!DOCTYPE html>
<html>
<head>
  <script> 
    var user = <%= current_user.to_json(only: [:name, :id]).html_safe %>;
    var token = <%= form_authenticity_token.to_json.html_safe %>;
  </script>
  <script type="text/javascript" src="//use.typekit.net/wfq0vai.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  <link rel="shortcut icon" href="/favicon.ico/"/>
  <title>Balderdash</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar">
    <div class="container">
  <ul class="list-inline">
    <li id="logo"><a href="/games"> Balderdash</a></li>
    <li class="special-hide">Not just another bullshit game</li>
      <%  if current_user %>
    <li class="pull-right" ><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
		<li class="pull-right special-hide">Welcome, <%= current_user.name %>!</li>
    <% end %>
    
   </ul>
	</div>
</nav>

<div class="container body">
  <%= yield %>

  </div>
</body>
</html>
